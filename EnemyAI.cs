using UnityEngine;

public class EnemyAI : MonoBehaviour
{
    public Transform player;
    public float moveSpeed;

    // Update is called once per frame
    void FixedUpdate()
    {
        //if (transform.position.y - player.position.y != .5f)
        //    transform.position = Vector3.MoveTowards(transform.position, new Vector2(transform.position.x, player.position.y + .5f), moveSpeed*.1f);
        //else 
            
    }
}
